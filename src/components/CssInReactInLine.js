

const CssInReactInLine = () => {
  return (
    <div style={{backgroundColor:'yellow'}}>
        <h1 style={{fontSize:'50px',color:'blue'}}>CSS in React with inline styling</h1>
        <p style={{textAlign:"center",color:"green"}}>This is just beginning of using CSS in React</p>
    </div>
  );
};

export default CssInReactInLine;